<div class="room">
  <% if room.picture.attached?  %>
    <div class="room-picture"><%= image_tag url_for(room.picture) %></div>
  <% end %>
  <div class="room-spec">
    <h2><%= room.name %></h2>
    <p>max. capacity <%= room.size %> person / <%= room.price %>€ per day </p>
  </div>
  <div class="room-services">
    <ul>Services
      <% room.services.each do |room_service| %>
      <li>
        <%= room_service.name %>
      </li>
      <% end %>
    </ul>
  </div>
</div>
